This log is append-only. Entries are never edited; new rows are appended at the bottom with a date.

## 11. Decision log

Append with date. Do not edit prior entries.

| Date | Decision | Rationale |
|---|---|---|
| 2026-08-04 | Outcome is video-level log(views) with channel and calendar-month FE, not channel-level subscribers | Subscriber counts are rounded to three significant figures. Month FE absorb the view-accrual bias common to treated and control |
| 2026-08-04 | CORE excluded from the estimation sample | Twitch-first, fails the inclusion rule. 9-week post-period. Channel terminated one day after launch |
| 2026-08-04 | Collaborator tags used for validation only | Approximately 12 months of coverage, not in the API, and consent-based so endogenous to the measured relationship |
| 2026-08-04 | YouNiverse reduced to `df_channels_en.tsv.gz` as sampling frame | The video-level design removes the need for the weekly series as a primary outcome |
| 2026-08-04 | No short paper. README, METHODS.md, and a 1,200-word write-up instead | The paper format invites evaluation against academic standards this design cannot meet |
| 2026-08-04 | Owner's 11-year viewer history recorded as positionality, not just motivation | Long-term attachment to one group in the sample is a prior. Disclosing it is the reason the hypotheses are frozen before estimation |
| 2026-08-04 | Karpathy loop logs committed, per-attempt artifacts ignored | The log is the evidence the metric was optimised against a fixed evaluation set. Attempt artifacts are reproducible from it |
| 2026-08-04 | Ponytail review runs before every phase commit and after every loop closes. `.claude/` gitignored | Loops add code without removing it. Deletion is a separate pass. Agent tooling is not part of the deliverable and would conflict with the no-attribution rule |
| 2026-08-04 | Repository is public from `chore: init` | Unlimited scheduled Actions minutes. An honest commit history from scaffolding to finding is part of what the project demonstrates |
| 2026-08-04 | API key uses API restriction only, no application restriction | IP restriction breaks GitHub Actions runners, which use rotating IPs. Key stored in `.env` locally and as a repository secret for Actions |
| 2026-08-04 | Environment is the existing anaconda `course` env (Python 3.13.11), not a uv-managed venv | Owner instruction. Six deps installed additively into it; dry-run confirmed no change to existing env packages. pandas left at 3.0.3, not downgraded, because the env is shared with coursework. Runtime risk on pandas 3 for differences, statsmodels, pandera noted for Phase 6 |
| 2026-08-04 | Accidental empty git repo at `~/Documents` removed before `git init` | 0 commits, no remote, ~11 GiB orphaned objects from a one-off `git add`. Removing it prevented a nested repo and reclaimed disk |
| 2026-08-04 | Control matching drops country | YouNiverse `df_channels_en.tsv.gz` has no country field. The frame is English-language and every group is UK or US, so country is partly implicit. Match on subscriber band, creation year, and category |
| 2026-08-04 | Inclusion and exclusion test rewritten from platform-based to mechanism-based (Sections 2.1, 2.2) | The old test excluded groups for being Twitch-first. That excluded AMP and OfflineTV inconsistently. New test: excluded only if the group channel does not publish original long-form video as a major product. Owner instruction, overriding the FROZEN marker |
| 2026-08-04 | AMP retained, OfflineTV added under the amended test | Both operate qualifying long-form group channels. OfflineTV moved from exclusions to the cohort with 8 members resolved |
| 2026-08-04 | OTK excluded, ruling final | Measured against the owner rule (include only if majority-original and members not contracted). OTK signs creators (Cyr, Tectone, Emiru) and runs Mythic Talent, an agency for 65+ creators, so members are contracted. The contracted condition fails, so clip-ratio is moot. Management-brand exclusion under the amended 2.2 |
| 2026-08-04 | Vlog Squad excluded from cohort and Q6 | No distinct group channel; content on David Dobrik's personal channel. Fails 2.1.1 |
| 2026-08-04 | Team 10 reduced to case-study only | Incomplete rotating roster, defunct group channel, poorly documented member dates. Retained only as the Q6 collapse case. Founding corrected to 2016-08-05 from press, replacing the plan's 2017 |
| 2026-08-04 | Q6 reduced to two cases, KSI and Team 10 | Vlog Squad removed from the sample |
| 2026-08-04 | Clustering: group level via wild cluster bootstrap | About 6 treated groups, three joining in 2019 across eight months. Group-level clustering is invalid at few clusters and channel-level understates SEs when treatment is assigned by group. Recorded in `docs/HYPOTHESES.md`, confirmed by owner |
| 2026-08-04 | AMP founding corrected to October 2019 | The 2019-06-01 value was a year-only placeholder. Press (Yahoo, Rolling Stone) and the AMP wiki date the co-founding to October 2019. Founder join dates tightened to 2019-10-01 at month precision, the level that can move the event-study timing |
| 2026-08-04 | Expected signs pre-registered in `docs/HYPOTHESES.md` | Q1 rises, Q2 falls, Q3 falls, Q4 near zero and imprecise, Q5 negative for large and positive for small. DiD covariates set to none beyond channel and calendar-month FE |
| 2026-08-04 | Treated cohort and control list FROZEN | `config/cohort_groups.csv` and `config/cohort_controls.csv` frozen at commit 0d44234. Regenerating the control list after seeing results is forbidden per Section 4.3. Reopen only on an explicit owner instruction |
| 2026-08-05 | Collector uses raw urllib, not google-api-python-client | Matches the existing `verify_cohort.py` pattern, stdlib only, no SDK. The listed `google-api-python-client` dependency is now unused; flagged for later removal, not removed this phase |
| 2026-08-05 | Uploads playlist derived as UC->UU, not fetched | The uploads playlist ID is the channel ID with the UC prefix replaced by UU. Zero quota, and it keeps `search.list` out of the pull. Fallback to one `channels.list` call if a channel ever fails the transform |
| 2026-08-05 | CORE snapshot channels left mostly unresolved | Only FaZe Adapt (`@Adapt`) verified. Handle guesses for Silky, JasonTheWeen, Lacy, Marlon, Stable Ronaldo resolve to wrong or missing channels, and `search.list` is banned. Snapshotting unverified IDs would corrupt the follow-up series, so the five rows wait for a citable handle. Revisit with CORE at 2027-06-01 |
| 2026-08-05 | treatment_date added, defined as max(join_date, group channel creation); Sidemen 2015-06-14, AMP 2020-01-24 | The mechanism under test is view capture by the group channel, which cannot operate before the channel exists. Sidemen carried 20 months and AMP 4 months of coded post-treatment with no group channel. Makes the date basis uniform across all five groups. join_date retained as the sourced fact. Control file unaffected: matching used control_subs_2019 for all treated units |
| 2026-08-05 | Minimum 12 months of pre-treatment channel history required for Q4 and Q5 | Replaces the case-by-case Agent 00 exclusion with an even rule. Excludes Agent 00 and Yvonnie, 30 of 32 retained. Yvonnie's channel was created on her join date and was incorrectly listed as a clean late joiner |
| 2026-08-05 | Observations after leave_date censored in Q4 and Q5 | Callaway and Sant'Anna assumes absorbing treatment. Four members exited (Fedmyster, Mopi, Pokimane, KSI); coding post-exit output as treated misassigns up to six years per member |
| 2026-08-05 | KSI claim provenance corrected to a YouTube community post reshared to Instagram story | The archived screenshot shows the YouTube Posts tab with an Instagram reshare sticker. Section 1.1 and BACKGROUND.md described the Instagram story as the origin |
| 2026-08-05 | OTK exclusion re-grounded on the amended 2.2 mechanism test; contracted-members condition withdrawn | The contracted condition appears nowhere in 2.1 or 2.2 and management-brand exclusion was retired with the platform-based test on 2026-08-04. Applied evenly it would also threaten AMP and OfflineTV, 14 of 34 treated members in files frozen at 0d44234. OTK excluded on group-channel composition, determined by the same inspection basis used to admit AMP and OfflineTV |
| 2026-08-05 | config/cohort_groups.csv amended additively at commit bd60262: added treatment_date, eligible_q4q5, censor_date, columns only, 43 rows unchanged | Owner instruction. Derived from existing sourced fields; join_date and leave_date retained unchanged. control file untouched, stays frozen at 0d44234. SHA recorded after the commit landed, matching how 0d44234 was recorded |
| 2026-08-05 | Sections 2, 3, 4, 11 extracted to tracked docs/SCOPE.md and docs/DECISIONS.md; PROJECT_PLAN.md gitignored as the working checklist | Pre-registration is only checkable if it is timestamped in tracked history. Section 4.3 forbids regenerating controls after seeing results and a reader cannot verify that against a local file. The phase checklist is working scratch. Phase 2 commits squashed from four to two; the config SHA citation updated accordingly |
| 2026-08-10 | Phase 3 cleaning shipped in two commits at a4b2623 (feat) and 4061a76 (docs). Age-filter reference date set to max(published_at) 2026-08-07; dedupe collapses exact video_id and exact (channel_id, title, duration_seconds) re-uploads only | Reference date taken from the data so re-runs are reproducible without a stored pull timestamp; it sits within 5 days of the actual pull. Exact-only dedupe avoids false merges of series parts with no validation set to check fuzzy matches. Durations parsed with pd.to_timedelta, no new dependency. Tests folded into the feat commit to hold the 2-commit ceiling |
| 2026-08-10 | Deleted-video coverage gap quantified at an upper bound of 0.174, N=138 archived IDs across 6 sampled channels, recorded in docs/coverage.md | Section 4.2 requires the unobservable deleted-video gap to be quantified. Wayback archives modern channel video grids sparsely (3 of 6 channels returned 0 IDs) and archived grids carry non-cohort recommendation links, so the rate is an upper bound on a small sample, not a correction applied to the estimate. All 24 absent IDs come from Dude Perfect, a contrast case excluded from estimation |
| 2026-08-10 | pandas 3.0.3 handled in the cleaning pipeline, shared course env left unchanged | pandera runs via the pandera.pandas import path; datetime defaults to microsecond resolution so schemas pin datetime64[us, UTC]; mixed fractional-second timestamps parsed with format=ISO8601. The pandas-3 runtime risk in the 2026-08-04 log entry did not require a downgrade for Phase 3 |
| 2026-08-11 | Phase 4 collaboration validation set built from viewing, not the native collaborator tag | The de-risk audit (14 recent long-form videos across all 5 ecosystems, including a guest-featuring Sidemen video) found a single-owner videoOwnerRenderer byline on every one and no collaborator tag structure, so the Section 6 tag-based set is not buildable. Ground truth switches to viewing the video, which stays independent of the description-link parser under test |
| 2026-08-11 | Viewing labels taken transcript-primary, frames pulled only for ambiguous cases | Calibration on one 38-minute video showed creator identification from sparse frames unreliable at the density that keeps a large sample feasible, while the caption transcript carries the signal through direct address. A cohort member counts as present when named at least 3 times (repeated address); 1 to 2 mentions are references, confirmed by cases such as "in the Simon charity match" (a past event, not Miniminter present). Frames resolve the ambiguous band. Sample is Sidemen-ecosystem only, so Q3 scope follows the final n per Section 12 |
| 2026-08-27 | Q4 and Q5 estimator changed from Callaway and Sant'Anna to the Sun and Abraham fallback | The pre-registered primary via the `differences` 0.3.0 package runs point estimates and the event study on pandas 3.0.5, but its group-level clustering path (`cluster_var`) is broken by a two-site pandas-3 API incompatibility inside the package. HYPOTHESES named Sun and Abraham as the fallback. It runs at the SCOPE 4.1 video level with never-treated and not-yet-treated comparisons, and an ecosystem-level wild cluster bootstrap with Webb weights. Specification otherwise unchanged; documented in the `src/models/did.py` docstring |
| 2026-09-03 | Phase 8 dashboard built (Stage 1) reading committed CSVs only; Streamlit Community Cloud deploy deferred; root manifest is `requirements.txt` | Owner elected to build Phase 8 rather than take the Section 12 four-week cut. `data/processed/*.parquet` is gitignored (videos.parquet 269 MB) and absent on Community Cloud, so the app reads only the 14 `outputs/tables/*.csv` and `data/snapshots/channel_stats.csv`, formatting each figure to the FINDINGS precision (STYLE rule B). The app runs no model and reads no estimate for tuning, so the render-and-verify QA is presentation QA, not a Karpathy loop (Section 8.3). requirements.txt chosen over environment.yml for the pip-based Cloud default. The Stage 1 app landed in the `feat(app): add streamlit dashboard` commit; deploy is Stage 2 |
| 2026-09-03 | Dashboard gains a Research questions page and its in-app copy is rewritten to the owner's academic register | Owner request. A sixth page lists Q1 through Q6 with the answer reached for each; the unanswerable Q3 and the KSI half of Q6 carry the reason they cannot be answered, and the change that would answer them is listed among the Conclusion suggestions. All prose across the pages is rewritten to a formal, purpose-first register with exact numbers and measured hedging, while every figure still matches docs/FINDINGS.md (STYLE rule B) and no estimate is recomputed. Folded into the Phase 8 `feat(app)` and `docs` commits |
